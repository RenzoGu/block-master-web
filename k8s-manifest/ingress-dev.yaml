apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: clmusicplace
  #annotations:
  #  nginx.ingress.kubernetes.io/rewrite-target: /
  # cambiar 20.72.99.142 por el IP de tu Ingress 
spec:
  ingressClassName: nginx
  rules:
  - host: clmusicplace.20.72.119.202.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service: 
            name: clmusicplace
            port:
              number: 80